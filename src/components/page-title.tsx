// todo: this will have the breadcrumb component for easier and accessible navigation

export const PageTitle = ({ title }: { title: string }) => {
  return <h1 className="text-2xl font-semibold tracking-tight">{title}</h1>
}
